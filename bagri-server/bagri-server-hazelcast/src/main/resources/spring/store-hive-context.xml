<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:hdp="http://www.springframework.org/schema/hadoop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://www.springframework.org/schema/hadoop http://www.springframework.org/schema/hadoop/spring-hadoop.xsd">

	<!-- set some common hive props in extra properties file -->
	<context:property-placeholder ignore-unresolvable="true" location="classpath:/${xdm.config.properties.file}"/>
	<context:mbean-export/>
    
	<hdp:hive-client-factory host="${xdm.schema.store.host}" port="${xdm.schema.store.port}" />
	<!-- Hive template wires automatically to 'hiveClientFactory'-->
	<hdp:hive-template />
	
	<!-- wire hive template into a bean -->
	<bean id="hiveCacheStore" class="com.bagri.xdm.cache.hazelcast.store.hive.HiveCacheStore">
	    <property name="hiveTemplate" ref="hiveTemplate" />
	</bean>

	<!--bean id="elementCacheStore" class="com.bagri.xdm.cache.hazelcast.store.XDMElementCacheStore">
	    <property name="mongoCacheStore" ref="mongoCacheStore" />
	</bean-->
</beans>